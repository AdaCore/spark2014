{
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,os; p=json.load(sys.stdin).get('tool_input',{}).get('file_path',''); root=os.environ.get('CLAUDE_PROJECT_DIR',''); rel=os.path.relpath(p,root) if root else p; print(p) if p.endswith(('.ads','.adb')) and (rel.startswith('src/') or rel.startswith('docs/ug/table_generator/')) else None\" | xargs -r gnatformat 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}

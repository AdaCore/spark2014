{
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; p=json.load(sys.stdin).get('tool_input',{}).get('file_path',''); print(p) if p.endswith(('.ads','.adb')) else None\" | xargs -r gnatformat 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}

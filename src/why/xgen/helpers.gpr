project Helpers is

   for Source_Dirs use (".", "..");

   for Main use ("xtree");

   for Object_Dir use "obj";

   for Exec_Dir use ".";

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-O0", "-gnata", "-gnatg");
   end Compiler;

end Helpers;

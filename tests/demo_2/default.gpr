project Default is

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-O2");
   end Compiler;

   package Ide is
      for Default_Switches ("examiner") use ("-flow_analysis=data", "-vcg", "-index_file=/home/sango/hi-lite-forge/tests/demo_1/sparkified/spark.idx");
   end Ide;

end Default;


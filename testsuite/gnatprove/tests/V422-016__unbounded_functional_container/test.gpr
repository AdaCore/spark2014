project Test extends "sparklib" is

   for Main use ("infinite_sequence.adb");

   for Object_Dir use "obj";

   package Compiler is
      for Default_Switches ("Ada") use
         ("-gnat2022", "-gnata", "-gnatwI",
          "-gnateDSPARK_BODY_MODE=On");
   end Compiler;

end Test;

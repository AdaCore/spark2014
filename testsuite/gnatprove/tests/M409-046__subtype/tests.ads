with Subty; use Subty;
package Tests is

   procedure Enum (V : in out Vowels);

   procedure Pos_Not_Ten (V : in out Positive_But_Not_Ten);

   procedure Pos_Hole (V : in out Positive_With_Hole);

   procedure Pos_Not_One (V : in out Positive_Not_One);

end Tests;

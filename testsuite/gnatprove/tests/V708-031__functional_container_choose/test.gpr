with "sparklib";

project Test is

   for Object_Dir use "obj";
   for Main use ("test.adb");

   package Builder is
   end Builder;

   package Compiler is
      for Switches ("ada") use ("-g");
   end Compiler;

   package Linker is
      for Switches ("ada") use ("-g");
   end Linker;

end Test;


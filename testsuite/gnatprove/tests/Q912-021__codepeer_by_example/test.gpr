project Test is

   package Compiler is
      for Default_Switches ("Ada") use ("-gnatws");
   end Compiler;

   package Builder is
      for Global_Configuration_Pragmas use "spark.adc";
   end Builder;

   package Prove is
      for Proof_Switches ("Ada") use
        ("--counterexamples=on", "--check-counterexamples=on");
      for Proof_Switches ("bad_assign_all_arr_unk.adb") use
        ("--counterexamples=on", "--check-counterexamples=off");
   end Prove;

end Test;

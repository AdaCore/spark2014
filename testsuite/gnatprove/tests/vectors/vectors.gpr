project Vectors is

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-O2");
   end Compiler;

   package Ide is
      for Default_Switches ("examiner") use
        ("-index_file=vectors.idx", "-config=system.shs");
   end Ide;

end Vectors;


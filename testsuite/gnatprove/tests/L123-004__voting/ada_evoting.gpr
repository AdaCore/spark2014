project Ada_EVoting is

   for Main use ("ada_evoting.adb");
   for Library_Interface use ();

   package Ide is
      for Vcs_Kind use "Git";
   end Ide;

   package Compiler is
      for Default_Switches ("ada") use ("-gnatwa", "-gnat12", "-gnato", "-gnatE");
   end Compiler;

   package Builder is
      for Default_Switches ("ada") use ("-j2");
   end Builder;

end Ada_EVoting;


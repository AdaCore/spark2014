project Test is

   for Source_Dirs use (".");
   for Object_Dir use "obj";

   package Compiler is
      -- Comment out the following line ("activate every optional warning
      --  except reasons for body") and an error occurs.
      for Default_Switches ("Ada") use ("-gnatw.e.Y");
   end Compiler;
end Test;

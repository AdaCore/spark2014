with "sparklib";

project Test is
   
   for Object_Dir use "obj";
   for Main use ("test.adb");

   package Builder is
      for Switches ("ada") use ("-gnata", "-gnat2022");
   end Builder;

end Test;


project IPStack_LWIP is
   Subdir := "src/core";
   for Library_Name use "lwip4";
   for Library_Kind use "static";

   for Source_Dirs use ();
   for Library_Dir use "../src/lwip4aip";

   for Externally_Built use "true";

end IPStack_LWIP;

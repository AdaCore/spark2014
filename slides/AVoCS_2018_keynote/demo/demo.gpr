project Demo is

   for Main use ("test_device.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnata", "-g", "-O0");
   end Compiler;

   package Builder is
      for Global_Configuration_Pragmas use "spark.adc";
   end Builder;

end Demo;

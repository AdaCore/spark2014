configuration project Why is
   for Default_Language use "Why";
   package Compiler is
      for Driver ("Why") use "why";
      for Required_Switches ("Why") use
         ("--multi-why", "--explain", "--fast-wp", "--default-locs", "--delete-old-vcs");
      for Object_File_Suffix ("Why") use "_ctx.why";
      for Language_Kind ("Why") use "file_based";
   end Compiler;

   package Naming is
      for Implementation_Suffix ("Why") use "__package.mlw";
   end Naming;

end Why;


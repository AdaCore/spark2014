package Stack_05
--# own Stack;
--# initializes Stack;
is
   procedure Push(X : in Integer);
   --# global in out Stack;

   procedure Pop(X : out Integer);
   --# global in out Stack;

end Stack_05;

package Stack_05
--# own State;
is
   procedure Push(X : in Integer);
   --# global in out State;

   procedure Pop(X : out Integer);
   --# global in out State;

   procedure Init;
   --# global    out State;

end Stack_05;

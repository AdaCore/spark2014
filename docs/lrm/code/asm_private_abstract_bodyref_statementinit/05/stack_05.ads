package Stack_05
--# own State;
--# initializes State;
is
   procedure Push(X : in Integer);
   --# global in out State;

   procedure Pop(X : out Integer);
   --# global in out State;

end Stack_05;

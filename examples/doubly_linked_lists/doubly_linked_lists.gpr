project Doubly_Linked_Lists is

   package Ide is
      for Default_Switches ("examiner") use ("-index_file=doubly_linked_lists.idx", "-fdl_identifiers=_fdl_");
   end Ide;

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-O2");
   end Compiler;

end Doubly_Linked_Lists;


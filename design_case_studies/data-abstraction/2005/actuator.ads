  package Actuator
  --# own out Outputs;
  is
     procedure TurnOn;
     --# global  out Outputs;
     --# derives Outputs from ;
     --
     -- Turns the heating on.

     procedure TurnOff;
     --# global  out Outputs;
     --# derives Outputs from ;
     --
     -- Turns the heating off.
  end Actuator;
